# 续火花修复验收记录

日期：2026-09-13，America/Los_Angeles。

工作目录：`/project/fire`。首轮修复基于 `dev` 分支的 `d4f1bdd`，已保存为提交 `c8f44ca`。
本记录区分代码回归验证与线上真实发送验收，提交代码本身不代表完成真实发送。

## 已完成的修改

| 范围 | 修复后的行为 |
| --- | --- |
| 好友匹配 | 从原始目标定位不冲突的身份，阻止同名昵称、备注链将一个人的目标扩展到另一个人的 ID；当前聊天仅通过标题区域的精确匹配确认。 |
| 发送前检查 | 校验当前聊天、保留已有草稿，输入后再次校验消息内容和聊天身份；输入未生效或会话改变时不按发送键。 |
| 提交与续传 | 发送前持久化 `pending`；页面核验通过后记录 `submitted`。结果不明确时保留 `pending`，不计为完成，不自动再次提交；最终写盘失败只重试写盘。 |
| 收件人状态 | 已确认的稳定身份使用独立记录键，并保存 `recipient_identity` 与原始 `target`。同一人的 ID、昵称和备注共享状态，`pending` 优先；兼容旧原文键及 `sent_at` 记录。 |
| 代理错误 | 上游请求超时或响应回填失败后终止浏览器请求，避免再次通过浏览器重放可能已受理的发送请求。 |
| 线上超时补修 | 隐藏会话跳过，点击等待不超过 5 秒，同一失败候选不在列表中反复点击；输入事务保留同一个编辑器节点，避免占位属性消失后换行/回车定位失效。 |
| 完成统计 | 所有未完成目标都进入失败汇总，包括始终没有被选中的好友；同一人使用多个已确认别名时，按已有提交记录完成统计，不重复发送。 |
| 工作流 | 两个实际执行 `dev` 代码的工作流统一状态缓存前缀，真实发送仅从默认分支触发；取消后仍尝试保存状态并上传备份。触发脚本先解析和校验默认分支。 |

## 验证结果

实际执行：

```sh
HUOHUA_BROWSER_TESTS=1 .venv/bin/python -m unittest discover -s tests -q
```

终端结果：

```text
Ran 121 tests in 23.363s

OK
```

121 项全部通过，零失败、零错误、零跳过。其中 11 项在本机完整的 Google Chrome 中执行
本地 HTML 夹具，浏览器上下文拦截网络请求，覆盖真实输入、回车、页面核验、失败提示、
旧消息、表情换行、标题身份及重启后的防重复行为。项目自带 Chromium 缺少框架文件，
因此这次浏览器验证使用系统 Chrome；没有使用真实账号进行发送。

新增的身份状态测试有 14 项，覆盖跨别名 `pending`、已提交别名跳过、重启后昵称变化、
API 尚未加载时的明确 ID 查询、不同收件人同名、昵称复用、账号隔离及最终写盘失败。
账号级离线夹具验证了仅继续未尝试目标、待核验目标保持失败状态和完整失败清单。

`git diff --check`、`bash -n deploy/trigger_huohua.sh` 和
`bash -n deploy/run-production.sh` 均通过。测试中的 `stale search input` 等报错来自
故意注入的故障夹具，不是测试失败。

## 线上验收边界

原线上运行编号为 `34763596176`。后续只读查询已取得其最终状态：`completed / cancelled`。
GitHub 返回的开始时间是 `2026-09-13T14:45:44Z`，最后更新时间是
`2026-09-13T16:16:03Z`（北京时间 9 月 14 日 00:16:03），运行头提交为
`fbbc05a87f1f8d5b1adcff0770b5c5b2921a5cec`。发送步骤被取消，缓存保存及附件上传步骤
均为 `skipped`，该运行的附件数为零。后续成功取得 3954 行工作流日志，保存在本地忽略
目录 `logs/online-run-34763596176.log`；没有“发送完成”记录，存在多次输入框按键超时，
后半程反复点击 `element is not visible` 的会话并等待 60000ms。

日志没有成功记录不等同于服务端零送达证明；不据此重跑全部目标。工作流新增手动
`diagnose_only=true` 入口，复用既有 `diagnoseFriendMatching` 路径进行生产环境的好友
匹配核验，不输入或发送消息。先前本地账号诊断未加载到可供真实页面核验的账号配置。

页面提交核验目前检查当前聊天、编辑器清空、新增匹配文本及失败提示，不是服务端送达
回执，也不是火花续成的证明。真实页面结构和最终送达仍需要线上验收。

状态缓存只有在保存、恢复成功时才提供跨运行保护；强制终止 runner、缓存丢失或存储故障
仍可能使状态无法恢复。这些情况下应先核对已有记录，再决定是否补发。

## GitHub 发布和生产诊断结果

已通过一次原子推送发布，并通过独立的 `git ls-remote` 读取确认：

- `dev` 代码版本：`2e869f9b617e6f7c2943dddd74e93567e6fe174d`。
- `main` 工作流版本：`c14c56bb81e06ce011dad953c22672d36c6a5d2c`。
- `main` 同步了两条工作流及 `deploy/trigger_huohua.sh`，工作流实际检出 `dev`。
- 独立服务器上的触发脚本没有在本轮变更；这里确认的是 GitHub 仓库和 Actions 发布。

手动诊断运行 `34791650341` 已完成，结论为 `success`。日志确认
`DIAGNOSE_FRIEND_MATCHING=1`，并明确进入不发送消息的好友匹配诊断路径。
结果为：目标 78 个、列表好友 45 个、匹配 36 个、未匹配 42 个。
这 42 个是“未在本次列表中匹配”，不等于已确认删除或无法搜索到。

诊断日志没有消息提交尝试；当日 UTC 2026-09-14 的状态缓存未命中，因此本次并未验证
从已有状态缓存恢复。日志附件上传成功，artifact ID 为 `10327648268`，大小 3308 字节。
本地保留完整日志：`logs/diagnostic-run-34791650341.log`。

工作流状态也已核查：正式工作流和 dev 手动工作流为 active，api 测试工作流为
disabled_manually。追加的工作流专项测试运行 10 项，全部通过。

真实发送仍未验收；诊断成功、列表匹配和页面可用性均不能作为 78 个目标已续火的证明。
